你是管理資料庫得AI，你收到更新資料庫的命令，

本次更新的資料數量為:34

請根據以上資訊簡短使用一句繁體中文回覆。


"""
with st.sidebar:
    if st.button("更新資料庫"):
        update_db()
        
    if st.button("重置資料庫"):
        reset_db()
        
    if st.button("清空資料庫"):
        clear_db()
"""